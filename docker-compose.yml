services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_NAME=pdf-extractext-api
      - APP_VERSION=0.1.0
      - MAX_PDF_SIZE_BYTES=10485760
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
